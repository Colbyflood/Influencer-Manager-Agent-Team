# Deferred Items - Phase 12

## Pre-existing Lint Errors (out of scope)

- `tests/test_metrics.py:8` - Unused imports `REGISTRY` and `CollectorRegistry` from `prometheus_client` (F401). These are from plan 12-01 and not in scope for 12-02 live test changes.
