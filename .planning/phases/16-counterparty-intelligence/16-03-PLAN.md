---
phase: 16-counterparty-intelligence
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/negotiation/counterparty/tone.py
  - src/negotiation/llm/prompts.py
  - src/negotiation/llm/composer.py
  - src/negotiation/llm/negotiation_loop.py
  - tests/counterparty/test_tone.py
  - tests/llm/test_composer.py
autonomous: true
requirements:
  - CPI-03

must_haves:
  truths:
    - "Agent uses transactional, data-backed tone when negotiating with talent managers"
    - "Agent uses relationship-driven, creative-alignment tone when negotiating with direct influencers"
    - "Tone guidance is injected into the LLM prompt alongside existing lever instructions"
    - "Default tone (no counterparty info) matches direct influencer style for backward compatibility"
  artifacts:
    - path: "src/negotiation/counterparty/tone.py"
      provides: "get_tone_guidance function returning tone instructions string"
      exports: ["get_tone_guidance"]
    - path: "tests/counterparty/test_tone.py"
      provides: "Tests for tone guidance generation"
      min_lines: 40
  key_links:
    - from: "src/negotiation/llm/composer.py"
      to: "src/negotiation/counterparty/tone.py"
      via: "Tone guidance passed as counterparty_context parameter"
      pattern: "counterparty_context"
    - from: "src/negotiation/llm/negotiation_loop.py"
      to: "src/negotiation/counterparty/tone.py"
      via: "get_tone_guidance called with counterparty_type from context"
      pattern: "get_tone_guidance"
    - from: "src/negotiation/llm/prompts.py"
      to: "counterparty_context placeholder"
      via: "New {counterparty_context} variable in EMAIL_COMPOSITION_USER_PROMPT"
      pattern: "counterparty_context"
---

<objective>
Implement tone adjustment so the agent adapts its negotiation communication style based on whether it is talking to a talent manager or a direct influencer.

Purpose: Talent managers respond to data, market rates, and professional efficiency. Direct influencers respond to creative alignment, partnership value, and relationship building. Using the wrong tone reduces negotiation effectiveness.

Output: get_tone_guidance function, updated prompts with counterparty context, composer wired to pass tone through.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-counterparty-intelligence/16-01-SUMMARY.md
@src/negotiation/llm/prompts.py
@src/negotiation/llm/composer.py
@src/negotiation/llm/negotiation_loop.py
@src/negotiation/counterparty/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tone guidance module and tests</name>
  <files>
    src/negotiation/counterparty/tone.py
    tests/counterparty/test_tone.py
  </files>
  <action>
    Create tone.py with get_tone_guidance function:

    get_tone_guidance(counterparty_type: str, agency_name: str | None = None) -> str

    For CounterpartyType.TALENT_MANAGER (counterparty_type == "talent_manager"):
    Return a multi-line instruction string (this is injected into the LLM prompt):
    ```
    COUNTERPARTY CONTEXT:
    You are negotiating with a talent manager/agency representative{agency_suffix}.
    Adapt your tone accordingly:
    - Be professional, concise, and direct -- managers handle many negotiations simultaneously
    - Lead with data: CPM benchmarks, market rates, campaign performance metrics
    - Frame proposals in business terms: ROI, deliverable value, cost efficiency
    - Avoid excessive flattery or creative-vision language -- focus on deal terms
    - Use industry-standard terminology (SOW, usage rights, deliverables, rate card)
    - Acknowledge their role: "I understand you're managing multiple campaigns..."
    ```
    Where {agency_suffix} is " from {agency_name}" if agency_name is provided, else "".

    For CounterpartyType.DIRECT_INFLUENCER (counterparty_type == "direct_influencer") or any other value:
    Return:
    ```
    COUNTERPARTY CONTEXT:
    You are negotiating directly with the influencer/creator.
    Adapt your tone accordingly:
    - Be warm, enthusiastic, and relationship-focused
    - Lead with creative alignment: how the brand fits their content style and audience
    - Emphasize partnership value: long-term relationship, creative freedom, brand alignment
    - Use creator-friendly language: "your audience", "your content", "collaboration"
    - Show genuine appreciation for their work and creative vision
    - Frame rate discussions around fair value for their unique audience and content quality
    ```

    For empty/None counterparty_type: return the direct_influencer version (backward compat default).

    Tests:
    - get_tone_guidance("talent_manager") contains "data", "CPM", "professional"
    - get_tone_guidance("talent_manager", "UTA") contains "UTA"
    - get_tone_guidance("direct_influencer") contains "warm", "creative", "partnership"
    - get_tone_guidance("") returns direct_influencer guidance (default)
    - get_tone_guidance(None) returns direct_influencer guidance (default) -- use type: str | None for param
    - Returned strings are non-empty
  </action>
  <verify>`python -m pytest tests/counterparty/test_tone.py -v` passes</verify>
  <done>get_tone_guidance returns appropriate tone instructions for both counterparty types and defaults safely</done>
</task>

<task type="auto">
  <name>Task 2: Wire tone guidance into composer, prompts, and negotiation loop</name>
  <files>
    src/negotiation/llm/prompts.py
    src/negotiation/llm/composer.py
    src/negotiation/llm/negotiation_loop.py
    tests/llm/test_composer.py
  </files>
  <action>
    1. Update EMAIL_COMPOSITION_USER_PROMPT in prompts.py:
       Add {counterparty_context} block AFTER the NEGOTIATION LEVER block and BEFORE the CONVERSATION HISTORY block:
       ```
       {counterparty_context}
       ```
       This keeps it clean -- just a placeholder that gets the full formatted string from get_tone_guidance.

    2. Update compose_counter_email in composer.py:
       - Add keyword-only parameter: counterparty_context: str = "" (after lever_instructions, for backward compat)
       - In user_text format call, add: counterparty_context=counterparty_context or "No specific counterparty context."
       - This mirrors the pattern used for lever_instructions (empty string default, descriptive fallback)

    3. Update process_influencer_reply in negotiation_loop.py:
       - After Step 8.5 (lever selection) and before Step 9 (compose email):
       - Import get_tone_guidance from negotiation.counterparty.tone
       - Get counterparty_type from negotiation_context.get("counterparty_type", "direct_influencer")
       - Get agency_name from negotiation_context.get("agency_name")
       - Call tone_guidance = get_tone_guidance(counterparty_type, agency_name)
       - Pass counterparty_context=tone_guidance to compose_counter_email call in Step 9

    4. Update existing composer tests in test_composer.py:
       - Existing tests should continue to pass (counterparty_context defaults to "")
       - Add one new test: test_compose_counter_email_with_counterparty_context that passes counterparty_context="COUNTERPARTY CONTEXT: test" and verifies the call succeeds (mock Anthropic client)
       - Verify the user prompt sent to the API contains the counterparty_context string

    Important: Do NOT modify the system prompt (EMAIL_COMPOSITION_SYSTEM_PROMPT). The counterparty context goes in the user prompt where it can vary per-request without invalidating the cached system prompt.
  </action>
  <verify>
    - `python -m pytest tests/llm/test_composer.py -v` -- all tests pass including new counterparty context test
    - `python -m pytest tests/llm/test_negotiation_loop.py -v` -- existing loop tests still pass
    - `python -m pytest tests/counterparty/ -v` -- all counterparty tests pass
    - `python -c "from negotiation.llm.composer import compose_counter_email; import inspect; assert 'counterparty_context' in inspect.signature(compose_counter_email).parameters"` -- parameter exists
  </verify>
  <done>
    - Composer accepts counterparty_context parameter and injects it into user prompt
    - Negotiation loop generates tone guidance from context's counterparty_type and passes to composer
    - Talent manager negotiations get data-backed, professional tone instructions
    - Direct influencer negotiations get warm, creative-alignment tone instructions
    - All existing tests pass unchanged (backward compatible defaults)
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/counterparty/ tests/llm/ -v` -- all pass
- Tone guidance for talent_manager includes data/CPM/professional language
- Tone guidance for direct_influencer includes warm/creative/partnership language
- Prompt template has {counterparty_context} placeholder
- Composer passes tone through to API call
- Negotiation loop reads counterparty_type from context and generates appropriate tone
</verification>

<success_criteria>
- Agent composes emails with manager-appropriate tone (transactional, data-backed) when counterparty_type is talent_manager
- Agent composes emails with influencer-appropriate tone (relationship-driven, creative) when counterparty_type is direct_influencer
- Default behavior (no counterparty_type in context) uses direct influencer tone for backward compatibility
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/16-counterparty-intelligence/16-03-SUMMARY.md`
</output>
