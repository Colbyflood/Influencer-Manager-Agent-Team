---
phase: 17-email-composition-and-style
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/negotiation/llm/composer.py
  - src/negotiation/llm/prompts.py
  - src/negotiation/llm/negotiation_loop.py
  - src/negotiation/llm/validation.py
  - tests/llm/test_agreement_composer.py
  - tests/llm/test_validation.py
  - tests/llm/test_negotiation_loop.py
autonomous: true
requirements:
  - EMAIL-07

must_haves:
  truths:
    - "Agent composes agreement confirmation emails with payment terms and explicit next steps"
    - "Agreement emails recap agreed deliverables, rate, usage rights, and timeline"
    - "Agreement emails include numbered next steps (SOW delivery, content brief, payment processing)"
    - "Validation gate checks agreement emails for payment term presence"
  artifacts:
    - path: "src/negotiation/llm/composer.py"
      provides: "compose_agreement_email function for deal confirmation"
      exports: ["compose_agreement_email"]
    - path: "src/negotiation/llm/prompts.py"
      provides: "AGREEMENT_CONFIRMATION_SYSTEM_PROMPT and AGREEMENT_CONFIRMATION_USER_PROMPT templates"
      contains: "AGREEMENT_CONFIRMATION"
    - path: "tests/llm/test_agreement_composer.py"
      provides: "Tests for agreement email composition"
      min_lines: 50
  key_links:
    - from: "src/negotiation/llm/negotiation_loop.py"
      to: "src/negotiation/llm/composer.py"
      via: "calls compose_agreement_email on accept"
      pattern: "compose_agreement_email"
    - from: "src/negotiation/llm/prompts.py"
      to: "src/negotiation/llm/composer.py"
      via: "AGREEMENT_CONFIRMATION prompts imported by composer"
      pattern: "AGREEMENT_CONFIRMATION_SYSTEM_PROMPT"
---

<objective>
Add agreement confirmation email composition with payment terms and next steps, and wire it into the negotiation loop's accept path.

Purpose: When an influencer accepts a deal, the agent must send a professional confirmation email recapping all agreed terms, stating payment terms, and listing explicit next steps (SOW, content brief, payment timeline). Currently the loop returns `{"action": "accept"}` without composing any email.
Output: Agreement email composer, dedicated prompts, validation updates, and wiring into the accept flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-email-composition-and-style/17-01-SUMMARY.md

@src/negotiation/llm/prompts.py
@src/negotiation/llm/composer.py
@src/negotiation/llm/negotiation_loop.py
@src/negotiation/llm/validation.py
@src/negotiation/llm/models.py
@src/negotiation/llm/sow_formatter.py
@knowledge_base/examples/positive_close.md
@tests/llm/test_composer.py
@tests/llm/test_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agreement confirmation prompts and composer function</name>
  <files>
    src/negotiation/llm/prompts.py
    src/negotiation/llm/composer.py
    tests/llm/test_agreement_composer.py
  </files>
  <action>
Add two new prompt templates to `src/negotiation/llm/prompts.py`:

1. `AGREEMENT_CONFIRMATION_SYSTEM_PROMPT`:
   - Same knowledge base injection pattern as EMAIL_COMPOSITION_SYSTEM_PROMPT
   - Rules specific to agreement confirmation:
     - "You are writing a deal confirmation email. Tone: warm, celebratory, action-oriented."
     - "Recap ALL agreed terms clearly: deliverables, rate, usage rights, timeline."
     - "Include payment terms: state when payment will be processed (e.g., 'within 30 days of content going live')."
     - "Include numbered next steps: 1) SOW for review and signature, 2) content brief and brand guidelines, 3) payment timeline."
     - "Write ONLY the email body. No subject line, no signature block."
     - "Address the influencer by their first name."
     - "Keep to 3-5 paragraphs. Be concise but thorough on terms."

2. `AGREEMENT_CONFIRMATION_USER_PROMPT`:
   ```
   Compose a deal confirmation email for this agreement:

   INFLUENCER: {influencer_name}
   PLATFORM: {platform}

   AGREED TERMS:
   {agreed_terms_block}

   PAYMENT TERMS: {payment_terms}

   {counterparty_context}

   CONVERSATION HISTORY:
   {negotiation_history}

   Write the confirmation email body now.
   ```

Add `compose_agreement_email` to `src/negotiation/llm/composer.py`:

```python
def compose_agreement_email(
    influencer_name: str,
    agreed_rate: str,
    deliverables_summary: str,
    usage_rights_summary: str | None,
    platform: str,
    payment_terms: str,
    knowledge_base_content: str,
    negotiation_history: str,
    client: Anthropic,
    model: str = COMPOSE_MODEL,
    counterparty_context: str = "",
) -> ComposedEmail:
```

- Build `agreed_terms_block` using `format_sow_block` from sow_formatter (no strikethrough -- use agreed rate directly)
- Format system prompt with knowledge base content (with cache_control)
- Format user prompt with all agreement parameters
- Default payment_terms to "within 30 days of content going live" if empty
- Return ComposedEmail with body and token usage

Create `tests/llm/test_agreement_composer.py`:
- `test_compose_agreement_email_returns_composed_email` -- basic return type check
- `test_agreement_system_prompt_includes_knowledge_base` -- KB injected into system
- `test_agreement_user_prompt_includes_agreed_terms` -- deliverables, rate, usage rights in prompt
- `test_agreement_user_prompt_includes_payment_terms` -- payment terms present
- `test_agreement_user_prompt_includes_next_steps_instruction` -- system prompt mentions next steps
- `test_agreement_default_payment_terms` -- empty string defaults to 30-day terms
- `test_agreement_counterparty_context_included` -- tone guidance passed through
  </action>
  <verify>
Run `python -m pytest tests/llm/test_agreement_composer.py -v` -- all tests pass.
Run `python -c "from negotiation.llm.composer import compose_agreement_email; print('import ok')"` -- import succeeds.
Run `python -c "from negotiation.llm.prompts import AGREEMENT_CONFIRMATION_SYSTEM_PROMPT, AGREEMENT_CONFIRMATION_USER_PROMPT; print('prompts ok')"` -- prompts importable.
  </verify>
  <done>Agreement email composition function exists with dedicated prompts. Prompts enforce term recap, payment terms, and numbered next steps. Tests verify all parameters flow through to the LLM call.</done>
</task>

<task type="auto">
  <name>Task 2: Wire agreement composer into negotiation loop and update validation</name>
  <files>
    src/negotiation/llm/negotiation_loop.py
    src/negotiation/llm/validation.py
    tests/llm/test_validation.py
    tests/llm/test_negotiation_loop.py
  </files>
  <action>
Update `process_influencer_reply` in `src/negotiation/llm/negotiation_loop.py`:

1. In the ACCEPT branch (Step 5, currently just returns `{"action": "accept"}`), add agreement email composition:
   - Load knowledge base with stage="agreed"
   - Get counterparty tone guidance (same pattern as counter-offer path)
   - Call `compose_agreement_email` with:
     - `agreed_rate` from negotiation_context (use `negotiation_context.get("last_offered_rate", "")` or fall back to calculating from next_cpm)
     - `deliverables_summary` from negotiation_context
     - `usage_rights_summary` from negotiation_context.get("usage_rights_summary")
     - `payment_terms` from negotiation_context.get("payment_terms", "within 30 days of content going live")
   - Validate the composed agreement email using `validate_composed_email`
   - Return updated dict: `{"action": "accept", "classification": classification, "email_body": composed.email_body}` on validation pass
   - On validation failure, still accept but include the draft for human review: `{"action": "accept", "email_body": composed.email_body, "validation_warnings": validation.failures}`

2. Pass the Anthropic `client` parameter through properly (it's already available in the function signature)

Update `validate_composed_email` in `src/negotiation/llm/validation.py`:
- Add optional `is_agreement: bool = False` parameter
- When `is_agreement=True`, add a check for payment-related terms: search for patterns like "payment", "paid", "processed", "invoice", "compensat" (case-insensitive). If none found, add a warning-severity failure: `check="payment_terms_missing"`, reason="Agreement email should mention payment terms"
- The hallucination check pattern for "usage rights" should be SKIPPED when `is_agreement=True` since agreement emails legitimately reference usage rights as agreed terms

Update `tests/llm/test_validation.py`:
- `test_agreement_mode_skips_usage_rights_hallucination` -- usage rights in agreement email does not trigger error when is_agreement=True
- `test_agreement_mode_warns_on_missing_payment_terms` -- warning when no payment language present
- `test_agreement_mode_passes_with_payment_terms` -- no warning when "payment" appears

Update `tests/llm/test_negotiation_loop.py` (if it exists, check first):
- Add test that ACCEPT intent now returns an `email_body` key in the result dict
- Verify the accept branch calls compose_agreement_email (mock check)
  </action>
  <verify>
Run `python -m pytest tests/llm/test_validation.py -v` -- all tests pass (existing + new).
Run `python -m pytest tests/llm/ -v` -- full llm test suite passes.
Run `python -m pytest tests/ -x -q` -- full test suite passes (no regressions).
Run `python -m mypy src/negotiation/llm/ --strict` -- no type errors.
Run `python -m ruff check src/negotiation/llm/` -- no lint issues.
  </verify>
  <done>Negotiation loop composes agreement confirmation emails on accept with payment terms and next steps. Validation gate has agreement-aware mode that checks for payment terms and permits usage rights references. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` -- full test suite passes
- `python -m mypy src/negotiation/llm/ --strict` -- no type errors
- `python -m ruff check src/negotiation/llm/` -- no lint issues
- Accept path in negotiation loop produces an email_body with payment terms
- Agreement validation warns on missing payment terms
- Agreement validation allows usage rights references
</verification>

<success_criteria>
- compose_agreement_email exists and composes confirmation emails with term recap, payment terms, and next steps
- process_influencer_reply accept branch now composes and returns an agreement email
- Validation gate has is_agreement mode that permits usage rights and warns on missing payment terms
- All existing tests pass without modification
- New tests cover agreement composition and validation
</success_criteria>

<output>
After completion, create `.planning/phases/17-email-composition-and-style/17-02-SUMMARY.md`
</output>
