---
phase: 22-per-campaign-sheet-routing
plan: 02
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - src/negotiation/campaign/ingestion.py
  - tests/campaign/test_ingestion.py
autonomous: true
requirements:
  - INGEST-03

must_haves:
  truths:
    - "Ingestion pipeline reads influencer_sheet_tab from parsed ClickUp fields and stores it on Campaign"
    - "Ingestion pipeline reads influencer_sheet_id from parsed ClickUp fields and stores it on Campaign"
    - "find_influencer calls use campaign's sheet tab instead of hardcoded Sheet1"
    - "find_influencer calls use campaign's spreadsheet ID override when present"
    - "When no tab/sheet override is set, behavior is identical to before (defaults to master sheet, Sheet1)"
  artifacts:
    - path: "src/negotiation/campaign/ingestion.py"
      provides: "Ingestion wiring that passes per-campaign tab/sheet to SheetsClient"
      contains: "influencer_sheet_tab"
    - path: "tests/campaign/test_ingestion.py"
      provides: "Tests verifying per-campaign sheet routing in ingestion"
      contains: "influencer_sheet_tab"
  key_links:
    - from: "src/negotiation/campaign/ingestion.py"
      to: "src/negotiation/sheets/client.py"
      via: "find_influencer called with worksheet_name and spreadsheet_key_override from campaign"
      pattern: "find_influencer.*worksheet_name.*spreadsheet_key_override"
    - from: "src/negotiation/campaign/ingestion.py"
      to: "src/negotiation/campaign/models.py"
      via: "build_campaign populates influencer_sheet_tab and influencer_sheet_id from parsed fields"
      pattern: "influencer_sheet_tab.*nested"
---

<objective>
Wire the ingestion pipeline to use per-campaign sheet tab and spreadsheet ID when looking up influencers, instead of the hardcoded "Sheet1" default.

Purpose: Complete the per-campaign sheet routing -- campaigns sourced from ClickUp now carry their own sheet config and the ingestion pipeline uses it.
Output: Updated ingestion.py with per-campaign routing and comprehensive tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-per-campaign-sheet-routing/22-01-SUMMARY.md
@src/negotiation/campaign/ingestion.py
@src/negotiation/campaign/models.py
@src/negotiation/sheets/client.py
@tests/campaign/test_ingestion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire build_campaign to populate sheet routing fields</name>
  <files>src/negotiation/campaign/ingestion.py</files>
  <action>
1. In `build_campaign()`, extract the two new fields from `nested` dict:
   - `influencer_sheet_tab = nested.get("influencer_sheet_tab")` -- may be None or a string
   - `influencer_sheet_id = nested.get("influencer_sheet_id")` -- may be None or a string
   If the value is an empty string, treat it as None (strip and check).

2. Pass both to the Campaign constructor:
   ```python
   influencer_sheet_tab=influencer_sheet_tab or None,
   influencer_sheet_id=influencer_sheet_id or None,
   ```

3. In `ingest_campaign()`, update the influencer lookup loop (Step 5) to pass per-campaign sheet config to `find_influencer`:
   - Determine `worksheet_name`: Use `campaign.influencer_sheet_tab` if set, otherwise default `"Sheet1"`
   - Determine `spreadsheet_key_override`: Use `campaign.influencer_sheet_id` if set, otherwise None
   - Change `sheets_client.find_influencer(influencer.name)` to:
     `sheets_client.find_influencer(influencer.name, worksheet_name=worksheet_name, spreadsheet_key_override=spreadsheet_key_override)`

4. Add a log line before the lookup loop: `logger.info("Influencer lookup config", tab=worksheet_name, sheet_override=bool(spreadsheet_key_override))`
  </action>
  <verify>Run `python -c "from negotiation.campaign.ingestion import build_campaign; c = build_campaign('t1', {'client_name': 'Test', 'budget': '1000', 'cpm_min': '20', 'cpm_max': '30', 'platform': 'instagram', 'timeline': 'Q1', 'influencers_raw': 'Alice', 'influencer_sheet_tab': 'CampaignX', 'influencer_sheet_id': 'abc123'}); assert c.influencer_sheet_tab == 'CampaignX'; assert c.influencer_sheet_id == 'abc123'; print('OK')"` to confirm build_campaign populates fields.</verify>
  <done>build_campaign extracts and populates influencer_sheet_tab and influencer_sheet_id. ingest_campaign passes these to find_influencer. Default behavior unchanged when fields are None.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for per-campaign sheet routing in ingestion</name>
  <files>tests/campaign/test_ingestion.py</files>
  <action>
1. Add a new test class `TestPerCampaignSheetRouting` with these test methods:

   a. `test_build_campaign_with_sheet_tab`: Call `build_campaign` with `influencer_sheet_tab: "MyTab"` in parsed_fields. Assert `campaign.influencer_sheet_tab == "MyTab"`.

   b. `test_build_campaign_with_sheet_id`: Call `build_campaign` with `influencer_sheet_id: "alt-spreadsheet-key"`. Assert `campaign.influencer_sheet_id == "alt-spreadsheet-key"`.

   c. `test_build_campaign_defaults_none`: Call `build_campaign` without sheet fields. Assert both `influencer_sheet_tab` and `influencer_sheet_id` are None.

   d. `test_build_campaign_empty_string_becomes_none`: Call `build_campaign` with `influencer_sheet_tab: ""` and `influencer_sheet_id: "  "`. Assert both are None.

   e. `test_ingest_passes_tab_to_find_influencer` (async): Mock `fetch_clickup_task` to return task data with a custom field "Influencer Sheet Tab" set to "CampaignX". Mock `sheets_client.find_influencer`. After ingestion, assert `sheets_client.find_influencer` was called with `worksheet_name="CampaignX"`.

   f. `test_ingest_passes_sheet_id_to_find_influencer` (async): Similar to above but with "Influencer Sheet ID" set to "alt-key-123". Assert `sheets_client.find_influencer` was called with `spreadsheet_key_override="alt-key-123"`.

   g. `test_ingest_defaults_to_sheet1_when_no_override` (async): Mock task data without sheet tab/id fields. Assert `sheets_client.find_influencer` was called with `worksheet_name="Sheet1"` and `spreadsheet_key_override=None`.

2. Follow existing test patterns in the file for mocking (MagicMock for sheets_client, AsyncMock or patch for fetch_clickup_task, etc.). Use the existing `_minimal_parsed_fields` helper if available, or create minimal parsed_fields dicts inline.
  </action>
  <verify>Run `python -m pytest tests/campaign/test_ingestion.py -v -k "PerCampaignSheet"` to confirm new tests pass. Run `python -m pytest tests/campaign/test_ingestion.py -v` to confirm no regressions.</verify>
  <done>Seven new tests verify: build_campaign populates sheet fields, empty strings become None, ingestion passes tab/sheet to find_influencer, and defaults work when no override is given. All existing ingestion tests pass unchanged.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/campaign/test_ingestion.py -v` -- all ingestion tests pass including new per-campaign sheet routing tests
- `python -m pytest --tb=short` -- full test suite passes
- `python -m mypy src/negotiation/campaign/ingestion.py` -- type checks pass
- `ruff check src/negotiation/campaign/ingestion.py` -- linting passes
</verification>

<success_criteria>
The ingestion pipeline uses per-campaign sheet tab and spreadsheet ID from the Campaign model when looking up influencers. When no override is provided, behavior is identical to before (master spreadsheet, "Sheet1" tab). Seven new tests verify all routing scenarios. Full test suite passes with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/22-per-campaign-sheet-routing/22-02-SUMMARY.md`
</output>
